#!/usr/bin/env php
<?php

/**
 * Put real bootstrap file include below. $serviceManager variable should be set somehow
 * 
 * @example require '/path/to/bootstrap'; // $serviceManager variable set inside of bootstrap
 * @example $serviceManager = require '/path/to/bootstrap'; // $serviceManager variable returned by bootstrap (prefered)
 */
 
// require '/path/to/bootstrap';
// OR
// $serviceManager = require '/path/to/bootstrap';
// OR
// [your variant]

include('doctrine.php');
